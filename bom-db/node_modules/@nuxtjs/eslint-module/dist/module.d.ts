import { Module } from '@nuxt/types';
import { Options } from 'eslint-webpack-plugin';

interface ModuleOptions extends Partial<Options> {
}
declare const CONFIG_KEY = "eslint";
declare const nuxtModule: Module<ModuleOptions>;
declare module '@nuxt/types' {
    interface NuxtConfig {
        [CONFIG_KEY]?: ModuleOptions;
    }
    interface Configuration {
        [CONFIG_KEY]?: ModuleOptions;
    }
}

export { ModuleOptions, nuxtModule as default };
